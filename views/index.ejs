<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        zinc: {
                            900: '#18181b',
                            800: '#27272a'
                        }
                    }
                }
            }
        }
    </script>
</head>
<body>
    <div class="p-10 w-full min-h-screen bg-zinc-900 text-white">
        <h3 class="text-2xl font-bold mb-6">Create User</h3>
        <form method="POST" action="/users" class="max-w-md space-y-4">
            <div class="flex flex-col">
                <label class="mb-1 text-sm text-zinc-400">Username</label>
                <input class="px-4 py-2 rounded-md bg-zinc-800 border border-zinc-700 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 outline-none transition" 
                       type="text" name="username" required>
            </div>
            
            <div class="flex flex-col">
                <label class="mb-1 text-sm text-zinc-400">Email</label>
                <input class="px-4 py-2 rounded-md bg-zinc-800 border border-zinc-700 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 outline-none transition" 
                       type="email" name="email" required>
            </div>
            
            <div class="flex flex-col">
                <label class="mb-1 text-sm text-zinc-400">Password</label>
                <input class="px-4 py-2 rounded-md bg-zinc-800 border border-zinc-700 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 outline-none transition" 
                       type="password" name="password" required>
            </div>
            
            <div class="flex flex-col">
                <label class="mb-1 text-sm text-zinc-400">Age</label>
                <input class="px-4 py-2 rounded-md bg-zinc-800 border border-zinc-700 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 outline-none transition" 
                       type="number" name="age" min="13" max="120">
            </div>
            
            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-md font-medium transition mt-6">
                Create User
            </button>
        </form>
    </div>
</body>
</html>